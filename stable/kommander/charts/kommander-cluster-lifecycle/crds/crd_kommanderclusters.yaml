
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    helm.sh/hook: crd-install
    controller-gen.kubebuilder.io/version: v0.2.4
  creationTimestamp: null
  name: kommanderclusters.kommander.mesosphere.io
spec:
  additionalPrinterColumns:
  - JSONPath: .metadata.annotations.kommander\.mesosphere\.io/display-name
    name: Display name
    type: string
  - JSONPath: .status.phase
    name: Status
    type: string
  - JSONPath: .status.kubefedclusterRef.name
    name: Kubefed Cluster
    type: string
  - JSONPath: .metadata.creationTimestamp
    name: Age
    type: date
  group: kommander.mesosphere.io
  names:
    kind: KommanderCluster
    listKind: KommanderClusterList
    plural: kommanderclusters
    shortNames:
    - komm
    - komms
    - kommander
    - kommanders
    singular: kommandercluster
  preserveUnknownFields: false
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: KommanderCluster is the Schema for the kommander clusters API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: KommanderClusterSpec defines the desired state of Cluster
          properties:
            clusterRef:
              description: ClusterReference holds a single reference to clusters provisioned
                via Kommander. Only one field is allowed to be set. Currently, only
                Konvoy clusters are creatable, but this is left extensible for other
                provider types in the future.
              properties:
                konvoyCluster:
                  description: LocalObjectReference contains enough information to
                    let you locate the referenced object inside the same namespace.
                  properties:
                    name:
                      description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        TODO: Add other useful fields. apiVersion, kind, uid?'
                      type: string
                  type: object
              type: object
            kubeconfigRef:
              description: LocalObjectReference contains enough information to let
                you locate the referenced object inside the same namespace.
              properties:
                name:
                  description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    TODO: Add other useful fields. apiVersion, kind, uid?'
                  type: string
              type: object
          type: object
        status:
          description: KommanderClusterStatus defines the observed state of Cluster
          properties:
            clusterId:
              description: KubernetesClusterID is the stable cluster ID of the Kubernetes
                cluster that this Kommander cluster represents.
              type: string
            dextfaclientRef:
              description: DexTFAClientRef holds a reference to a DexClient provisioned
                for Traefik Forward Auth running on managed cluster.
              properties:
                apiVersion:
                  description: API version of the referent.
                  type: string
                fieldPath:
                  description: 'If referring to a piece of an object instead of an
                    entire object, this string should contain a valid JSON/Go field
                    access statement, such as desiredState.manifest.containers[2].
                    For example, if the object reference is to a container within
                    a pod, this would take on a value like: "spec.containers{name}"
                    (where "name" refers to the name of the container that triggered
                    the event) or if no container name is specified "spec.containers[2]"
                    (container with index 2 in this pod). This syntax is chosen only
                    to have some well-defined way of referencing a part of an object.
                    TODO: this design is not final and this field is subject to change
                    in the future.'
                  type: string
                kind:
                  description: 'Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                  type: string
                name:
                  description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                  type: string
                namespace:
                  description: 'Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/'
                  type: string
                resourceVersion:
                  description: 'Specific resourceVersion to which this reference is
                    made, if any. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency'
                  type: string
                uid:
                  description: 'UID of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids'
                  type: string
              type: object
            kubefedclusterRef:
              description: KubefedClusterRef holds a reference to a kubefedcluster
                in the kubefed system namespace.
              properties:
                name:
                  description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    TODO: Add other useful fields. apiVersion, kind, uid?'
                  type: string
              type: object
            phase:
              description: Phase represents the current phase of cluster actuation.
                E.g. Pending, Provisioning, Provisioned, Deleting, Failed, etc.
              type: string
            serviceEndpoints:
              additionalProperties:
                type: string
              description: ServiceEndpoints will be the addresses assigned to the
                Kubernetes exposed services
              type: object
            type:
              description: ClusterType represents the type of the cluster. E.g. Konvoy,
                EKS, GKE, etc.
              type: string
          type: object
      type: object
  version: v1beta1
  versions:
  - name: v1beta1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
